import React from "react";
import styles from "./logo.module.css";
/**
 * @author
 * @function Logo
 **/

const Logo = (props) => {
  return (
    <div className={styles.logo}>
      <a href="#">Suman</a>
    </div>
  );
};

export default Logo;
